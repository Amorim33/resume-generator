import { LayoutQuery$data } from '../modules/layout/__generated__/LayoutQuery.graphql';

/** The context object that is passed to the `Outlet` component. */
export type OutletContext = {
  queryRef: LayoutQuery$data;
};
